var inquirer= require("inquirer");
// Letter.js should not require any other files.

// A string value to store the underlying character for the letter
function Letters(character){
  var Letters=function(){///Source:W3resource/lettercheck.
  this.display=/^[A-Za-z]+$/.test(character);
  this.display= character;


  returnFunction();{//call()method w3schools.
    return /^[A-Za-z]+$/;
  }

// A boolean value that stores whether that letter has been guessed yet
 Letters.prototype.toString=newFunction ();{
   if (this.display===true){
     return this.character;
   }
   return"_"
 }
   // A function that returns the underlying character if the letter has been guessed, or a placeholder (like an underscore) if the letter has not been guessed
   
   Letters.prototype.solution= firstFunction ();{
     return this.character;

    Letters.prototype.guess= secondFunction(guessedChar);{
      if(guessedChar.letter()===this.character.letter()){
        this.display=true;
        return true;

      }
//all other return false
      return false;

    }
     
   }
  }}
